Steps to run all 8 queries

  Step 0 — Prerequisites (one-time)

  cd D:\sjk\eagv3\s7\s7code
  uv sync                      # install all dependencies

  - .env must have at minimum TAVILY_API_KEY and GEMINI_API_KEY (decision/perception both use provider="g")
  - Verify D:\sjk\eagv3\llm_gatewayV7 exists (see flag above); if it does not, fix gateway.py

  Step 1 — Pre-run cleanup

  The sandbox already has mom_birthday.txt and two reminder files from a prior C run. create_file will error
  if they exist. Delete them:

  Remove-Item D:\sjk\eagv3\s7\s7code\sandbox\mom_birthday.txt
  Remove-Item D:\sjk\eagv3\s7\s7code\sandbox\reminder_*.txt

  State is already clean (no memory.json, no FAISS files in state/).

  Step 2 — Start the gateway (in a separate terminal, leave running)

  cd D:\sjk\eagv3\s7\s7code\llm_gatewayV7   # or wherever gateway.py resolves to
  uv run python main.py

  (Or let ensure_gateway() auto-start it on the first agent7.py run — but starting it manually avoids the
  45-second wait on every query.)

  ---
  Step 3 — Run queries in this exact order

  All commands run from D:\sjk\eagv3\s7\s7code.

  A (3 iterations — fresh state):
  uv run agent7.py "Fetch https://en.wikipedia.org/wiki/Claude_Shannon and tell me his birth date, death date, and three key contributions to information theory."

  B (8 iterations):
  uv run agent7.py "Find 3 family-friendly things to do in Tokyo this weekend. Check Saturday's weather forecast there and tell me which one is most appropriate."

  C Run 1 (4 iterations — writes mom's birthday to persistent memory):
  uv run agent7.py "My mom's birthday is 15 May 2026. Remember that and create reminders for two weeks before and on the day."

  C Run 2 (3 iterations, 0 tool calls — reads from FAISS; must happen before any state clear):
  uv run agent7.py "When is mom's birthday?"

  D (6 iterations):
  uv run agent7.py "Search for 'Python asyncio best practices', read the top 3 results, and give me a short numbered list of the advice they agree on."

  Clear state — E needs a fresh FAISS index to produce the expected chunk counts:
  uv run python -c "import memory; memory.clear()"

  E (5 iterations — indexes attention.md, answers from FAISS):
  uv run agent7.py "Index the file papers/attention.md and tell me what the three key contributions of the Transformer architecture are according to this paper."

  Clear state — F Run 1 needs a clean index so it produces exactly 15 chunks across 5 papers:
  uv run python -c "import memory; memory.clear()"

  F Run 1 (11 iterations — indexes all 5 .md files, builds corpus):
  uv run agent7.py "Index every .md file under papers/. Confirm how many chunks were indexed in total."

  F Run 2 (3 iterations — fresh process, persisted FAISS on disk; do NOT clear state):
  uv run agent7.py "Across the papers I have indexed, what do they say about chain-of-thought reasoning?"

  G (4 iterations — vector beats keyword; same persisted state from F):
  uv run agent7.py "Across these papers, how do they handle the credit assignment problem?"

  H (3 iterations — cross-document synthesis; same persisted state from F):
  uv run agent7.py "Compare how the ReAct paper and the Chain-of-Thought paper differ in their treatment of
  intermediate reasoning."

  ---
  Critical ordering constraints

  ┌──────────────────────────────────────┬────────────────────────────────────────────────────────────────┐
  │                 Rule                 │                             Reason                             │
  ├──────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ C Run 2 before any state clear       │ The mom's birthday FAISS vector is deleted by memory.clear()   │
  ├──────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ State clear before E                 │ Prevents duplicate attention.md chunks skewing iteration count │
  ├──────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ State clear before F Run 1           │ Produces the exact 15-chunk corpus G and H depend on           │
  ├──────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ No state clear between F Run 1, F    │ They all use the same persisted corpus — this is the           │
  │ Run 2, G, H                          │ architectural point of F                                       │
  └──────────────────────────────────────┴───────────────────────────
  
  